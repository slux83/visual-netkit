To compile this program, you have to install Qt >= 4.4 inside your Linux Box

When you are inside the application root directory, run the script `build.sh`.
This bash script build all plugins and the main application and run it!

All binary stuffs are located inside bin/VisualNetkit (the application bin)
and /bin/plugins/*.so (the plugins).

Enjoy!
